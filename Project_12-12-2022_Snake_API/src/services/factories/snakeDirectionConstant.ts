import { direction } from '../../domain/types';
export const directions = ["LEFT", "UP", "RIGHT", "DOWN"];
export const minimunAxis = 0;
export const node = 1;
export const initialDirection: direction = 'UP';
